-
  action: "add_triggers"
  triggers:
    t1:
        params:
          condition: "cycle() % 25 == 0"
          actions_file : "field.yaml"
    t2:
      params:
        condition: "cycle() % 25 == 0"
        actions_file : "visnek.yaml"
    t3:
      params:
        condition: "cycle() % 25 == 0"
        actions_file : "cinema.yaml"
    t4:
      params:
        condition: "cycle() % 25 == 0"
        actions_file : "trame.yaml"
